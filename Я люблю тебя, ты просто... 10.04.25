import time
import sys
import os

def print_pink(text, delay=0.04):
    """Выводит текст розовым цветом побуквенно с задержкой."""
    PINK = "\033[95m"  # Код ANSI для розового цвета
    RESET = "\033[0m"  # Код ANSI для сброса цвета

    for char in text:
        sys.stdout.write(PINK + char + RESET)
        sys.stdout.flush()
        time.sleep(delay)
    print()  # Переходим на новую строку после завершения

def print_lyrics(line_delay=0.04, pause_duration=1.0):
    """Выводит текст песни с настраиваемой задержкой и паузой."""
    song_text = """
В свете фонаря блестит асфальт
Я люблю тебя, ты просто знай!
Сдаётся мне, теперь это надолго
Гори, гори, не догорай
Без тебя жизни нет, я не могу найти
Сколько ни жмись к стене, сквозь неё не пройти
Ни одного пути грешной души в зиме
Без тебя смысла нет, без тебя жизни нет
Сколько ни жмись к стене, сквозь неё не пройти
Без тебя жизни нет, я не могу найти
Ни одного пути грешной души в зиме
Без тебя смысла нет, без тебя жизни нет
"""

    lines = song_text.strip().splitlines()

    for line in lines:
        print_pink(line, delay=line_delay)
        time.sleep(pause_duration)  # Пауза между строками


    # Выводим текст песни.
    # print_lyrics() # Старый вызов
    print_lyrics(line_delay=0.03, pause_duration=1.5)  # Новый вызов с параметрами
